/**
 * Placeholder — the real upload flow lives in app/upload.tsx (modal).
 * This file exists so the tab slot is valid; it's never actually shown
 * because PlusButton navigates to the modal route instead.
 */
import { View } from "react-native";

export default function UploadTabPlaceholder() {
  return <View style={{ flex: 1, backgroundColor: "#000" }} />;
}
